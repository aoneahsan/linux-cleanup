---
name: Bug report
about: Something didn't work as expected
title: "[bug] "
labels: bug
---

**What happened**
A clear description of the bug.

**What you expected**
What you thought should happen instead.

**Command you ran**
```
# e.g. linux-cleanup --scan
```

**Environment**
- Linux distribution + version:
- Bash version (`bash --version | head -1`):
- linux-cleanup version (`linux-cleanup --version`):
- `jq` installed? (yes/no):

**Logs (optional)**
Attach the relevant log from `~/.linux-cleanup/logs/` or a `--debug-bundle`
(review it first — it contains `$HOME` paths and cache sizes).
